import { StyleSheet } from "react-native";

export default StyleSheet.create({
    container: {
        alignItems: "center",
        justifyContent: 'center',
        padding: 16,
        flex: 1,
    },
    txtLoading: {
        fontWeight: 'bold',
        fontSize: 24,
    },
    progesBar: { 
        bounciness: 1, 
    },
    next: {
        alignItems: "center",
        justifyContent: 'center',
    }
}
)